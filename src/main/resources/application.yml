spring:
  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    show-sql: true

  datasource:
    url: jdbc:postgresql://localhost:5432/rooftop-event-booking
    username: demo
    password: demo123
    driverClassName: org.postgresql.Driver

  flyway:
    enabled: true
    username: demo
    password: demo123

server:
  servlet:
    context-path : /api/v1

